<?php

namespace {{ namespace }};

use Laravilt\Actions\Action;
use Laravilt\Forms\Components\TextInput;

class {{ class }} extends Action
{
    /**
     * Configure the action.
     */
    protected function setUp(): void
    {
        $this->label('{{ actionLabel }}');

        // Configure modal with form
        $this->requiresConfirmation()
            ->modalHeading('{{ actionLabel }}')
            ->modalDescription('Please fill in the required information.')
            ->modalSubmitActionLabel('Submit')
            ->form($this->getFormSchema());

        // Run handle() with the submitted form data
        $this->action(fn ($record = null, array $data = []) => $this->handle($record, $data));
{{ authorization }}
        // $this->icon('heroicon-o-check');
        // $this->color('primary');
    }

    /**
     * Get the form schema for the modal.
     */
    protected function getFormSchema(): array
    {
        return [
            TextInput::make('name')
                ->label('Name')
                ->required()
                ->maxLength(255),

            // Add more form fields here
        ];
    }
{{ authorizationMethod }}
    /**
     * Execute the action with form data.
     */
    public function handle(mixed $record = null, array $data = []): mixed
    {
        // Access form data via $data array
        // Example: $name = $data['name'];

        // Implement your action logic here

        return null;
    }
}
